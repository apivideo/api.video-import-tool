import type { NextPage } from 'next';
import VideoSourceSelector from '../../components/VideoSourceSelector';

const VideoSelection: NextPage = () => {

    return <VideoSourceSelector />
}

export default VideoSelection
